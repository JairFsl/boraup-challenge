export enum SECURE_STORE_KEYS {
  CURRENT_USER_DATA = "userData", // Informações do usuário que estiver logado
  TOKEN = "token", // Token JWT
  REFRESH_TOKEN = "refreshToken", // Token para renovar a sessão do usuário
}